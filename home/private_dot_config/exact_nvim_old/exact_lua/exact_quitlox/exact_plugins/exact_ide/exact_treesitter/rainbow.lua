return {
    url = "https://gitlab.com/HiPhish/rainbow-delimiters.nvim",
    version = "",
    -- FIXME: BUG. Something is wrong in the query for helpdocs. Steps to reproduce/test:
    --      - ex: "h lazy.nvim.txt"
    --      - search for after using: "/after"
    --      - hit next five times (all occurences)
    --      - > error
    enabled = false,
    lazy = false,
}
